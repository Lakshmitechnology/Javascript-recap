var num1 = 50;
var num2 = 100;

let val;

val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 % num2;
val = num1 / num2;

// console.log(val);
// Math? Object
val = Math.PI;

val = Math.round(2.4); //it take mothen 5 it takes uper number (2.6) ans=3
val = Math.ceil(2.4); //only upernumber
val = Math.floor(4.1); //round number
val = Math.sqrt(81);
val = Math.abs(-5); //positive val
val = Math.min(-9, 1, 2, 36, 4, 78, 5, 1, 0);
val = Math.max(-9, 1, 2, 36, 4, 78, 5, 1, 0);
val = Math.random(1223654);

val = Math.floor(Math.random() * 10);
console.log(val);
